- defaults:
  link:
    relink: true
    create: true
    force: true


- clean: ['~']

- shell:
  - git submodule update --init --recursive

- link: 
  ~/.zshrc: zsh/zshrc
  ~/.editorconfig: editorconfig/editorconfig
